name: km3net_cta_env
channels:
  - default
  - conda-forge
dependencies:
  - python=3.12
  - cython
  - gammapy=2.0
  - pip
  - pip:
      - astropy>=7.1
      - numpy>=2.3.4
      - pandas>=2.3.3
      - matplotlib>=3.10
      - regions
      - iminuit
      - naima
      - nbconvert
      - ipython
      - ipykernel
      - tqdm
